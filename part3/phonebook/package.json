{
  "name": "phonebook-backend",
  "version": "0.0.1",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "build:ui": "@powershell Remove-Item -Recurse -Force dist && cd ../phonebook-frontend && npm run build && @powershell Copy-Item dist -Recurse ../phonebook",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "elvastra",
  "license": "MIT",
  "description": "",
  "dependencies": {
    "express": "^5.1.0",
    "morgan": "^1.10.0"
  }
}
